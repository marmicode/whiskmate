name: Test
on:
  push:

env:
  # Using Nx Cloud is safer
  NX_REJECT_UNKNOWN_LOCAL_CACHE: 0

jobs:
  build:
    name: 📦 Build
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Setup
        uses: ./.github/actions/setup
      - run: pnpm nx run-many --parallel=4 -t build

  lint:
    name: 🧹 Lint
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Setup
        uses: ./.github/actions/setup
      - run: pnpm nx run-many --parallel=4 -t lint

  test:
    name: ✅ Test
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Setup
        uses: ./.github/actions/setup
      - run: pnpm nx run-many --parallel=4 -t test,vitest --pass-with-no-tests

  playwright-ct:
    name: 🎭 Playwright CT
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Setup
        uses: ./.github/actions/setup
      - name: Playwright browsers install
        run: pnpm playwright install --with-deps
      - run: pnpm nx run-many --parallel=4 -t test-ui --pass-with-no-tests
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: 🎭 Playwright Report
          path: dist/.playwright
          retention-days: 7

  deploy-gh-pages:
    name: 🚀 Deploy to GitHub Pages
    if: github.ref == 'refs/heads/testing-000-starter'
    runs-on: ubuntu-22.04
    needs:
      - build
      - lint
      - test
      - playwright-ct
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    permissions:
      pages: write
      id-token: write
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Setup
        uses: ./.github/actions/setup
      - name: Build docs
        run: pnpm nx build docusaurus
      - name: Upload Build Artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: docusaurus/build
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
